/target
Cargo.lock.bak

# Agent dotfiles created when running these tools against the arbox repo itself
.claude/
.codex/
.antigravitycli/

# Local wrangler state, created by `wrangler dev` in this repo
.wrangler/

# Agent scratch space (see CLAUDE.md)
TEMP/
